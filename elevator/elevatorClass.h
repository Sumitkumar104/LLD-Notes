#define ELEVATOR.H
#include<bits/stdc++.h>
using namespace std;

class elevator
{
    public:
    int currPosition;
    int direction;  // 0 means stop, 1 stop move up and -1 means move down
   
};

class outerButtonPannel{
    bool moveUp,moveDown;  
};

class innerButtonPannel{
     
}
